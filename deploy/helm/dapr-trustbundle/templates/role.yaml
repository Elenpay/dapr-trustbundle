{{- if and .Values.rbac.create (not .Values.rbac.useClusterRole) }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "dapr-trustbundle.fullname" . }}-manager-role
  namespace: {{ .Values.controller.targetNamespace }}
  labels:
    {{- include "dapr-trustbundle.labels" . | nindent 4 }}
rules:
# Permission to list secrets and configmaps in namespace (required for controller-runtime cache)
- apiGroups:
  - ""
  resources:
  - secrets
  - configmaps
  verbs:
  - list
  - watch
# Permission to read the source secret by name
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  resourceNames:
  - {{ .Values.controller.sourceSecretName }}
# Permission to create secrets and configmaps (no resourceNames needed for create)
- apiGroups:
  - ""
  resources:
  - secrets
  - configmaps
  verbs:
  - create
  - delete
# Permission to manage the target secret and configmap by name
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
  - patch
  - update
  resourceNames:
  - dapr-trust-bundle
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - patch
  - update
  resourceNames:
  - dapr-trust-bundle
# Permission to update finalizers on specific secrets
- apiGroups:
  - ""
  resources:
  - secrets/finalizers
  verbs:
  - update
  resourceNames:
  - {{ .Values.controller.sourceSecretName }}
  - dapr-trust-bundle
# Permission to update status on specific secrets
- apiGroups:
  - ""
  resources:
  - secrets/status
  verbs:
  - get
  - patch
  - update
  resourceNames:
  - {{ .Values.controller.sourceSecretName }}
  - dapr-trust-bundle
{{- with .Values.rbac.additionalRules }}
{{- toYaml . | nindent 0 }}
{{- end }}
---
{{- end }}
{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "dapr-trustbundle.fullname" . }}-leader-election-role
  namespace: {{ include "dapr-trustbundle.namespace" . }}
  labels:
    {{- include "dapr-trustbundle.labels" . | nindent 4 }}
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
{{- end }}
